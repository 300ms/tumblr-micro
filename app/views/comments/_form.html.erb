<%= form_for([@post, @post.comments.build]) do |x| %>
  <p>
    <%= x.label :name %>
    <%= x.text_field :name %>
  </p>

  <p>
    <%= x.label :body %>
    <%= x.text_area :body %>
  </p>

  <p>
    <%= x.submit %>
  </p>

<% end %>
